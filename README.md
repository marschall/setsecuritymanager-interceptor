setSecurityManager Interceptor Agent
=====================================

A Java agent for intercepting System.setSecurityManager.

Currently only works on Java 8.

Usage
-----

```
-javaagent:/absolute/path/to/setsecuritymanager-interceptor-agent.jar
```
